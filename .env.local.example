# ================================================
# Environment Variables for MCE Email Chat
# ================================================
# Copy this file to .env.local and fill in your actual values

# ================================================
# 1. AI PROVIDER (REQUIRED - Choose ONE)
# ================================================

# Option A: Claude by Anthropic (Recommended)
# Get your API key from: https://console.anthropic.com/
# Pricing: https://www.anthropic.com/pricing
CLAUDE_API_KEY=sk-ant-api03-xxxxx

# Option B: OpenAI GPT (Alternative)
# Uncomment to use OpenAI instead of Claude
# Get your API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-xxxxx

# ================================================
# 2. MCE SERVER CONFIGURATION (REQUIRED)
# ================================================

# Your MCE API server URL (the one you deployed on Fly.io)
# This is the bridge between the chat interface and Salesforce MCE
MCE_SERVER_URL=https://salesforce-mce-api.fly.dev

# API Key for authenticating with your MCE server
# This should match the API_KEY you set when deploying your MCE server to Fly.io
# Generate a secure key: openssl rand -hex 32
MCE_API_KEY=your-secure-api-key-from-mce-server

# ================================================
# 3. SALESFORCE MCE CREDENTIALS (INFO ONLY)
# ================================================
# NOTE: These are NOT needed here if your MCE server is already configured
# Your MCE server (on Fly.io) already has these configured as secrets
# Listed here for reference only:
#
# MCE_SUBDOMAIN=your-subdomain         # Set in Fly.io secrets
# MCE_CLIENT_ID=your-client-id         # Set in Fly.io secrets
# MCE_CLIENT_SECRET=your-secret        # Set in Fly.io secrets
# MCE_DEFAULT_MID=your-business-unit   # Set in Fly.io secrets

# ================================================
# 4. OPTIONAL CONFIGURATION
# ================================================

# Default email sender information (optional)
# If not set, defaults to "Marketing Team" and "marketing@company.com"
DEFAULT_FROM_NAME=Your Company Name
DEFAULT_FROM_EMAIL=noreply@yourcompany.com

# Enable debug logging (optional, for development)
# Set to "true" to see detailed logs in browser console
DEBUG=false

# Rate limiting (optional)
# Maximum requests per minute to prevent abuse
MAX_REQUESTS_PER_MINUTE=30

# ================================================
# SUMMARY: What you actually need to set
# ================================================
# 1. CLAUDE_API_KEY - Get from Anthropic Console
# 2. MCE_SERVER_URL - Your Fly.io deployment URL
# 3. MCE_API_KEY - The API key you configured on Fly.io
# That's it! Only 3 required variables.